syntax = "proto3";

package transmitter;

option go_package = "/transmitter";

import "google/protobuf/empty.proto";

service Transmitter {
    rpc DataStream(google.protobuf.Empty) returns (stream Response);
}

message Response {
    string session_id = 1;
    double frequency = 2;
    int64 time = 3;
}